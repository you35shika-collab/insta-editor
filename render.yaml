services:
  - type: web
    name: nihonshu-stand
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_public:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
